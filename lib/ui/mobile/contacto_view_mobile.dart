import 'package:flutter/material.dart';

class ContactoViewMobile extends StatelessWidget {
  const ContactoViewMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      child: Center(
        child: Text('Contacto mobile'),
      ),
    );
  }
}
